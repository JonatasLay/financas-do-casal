Login assets expected by src/app/login/page.tsx:

- login-bg-mobile.png: portrait background for mobile
- login-bg-desktop.png: landscape background for desktop/browser

Save the attached background images with these exact names before deploying if they
are not already present in this folder.
